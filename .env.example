# EmailJS Configuration
# Get these values from https://www.emailjs.com/

# Your EmailJS Service ID (e.g., from Gmail, Outlook, etc.)
# Found in: EmailJS Dashboard → Email Services → Your Service → Service ID
VITE_EMAILJS_SERVICE_ID=service_txf4zy6

# Your EmailJS Template ID
# Found in: EmailJS Dashboard → Email Templates → Your Template → Template ID
VITE_EMAILJS_TEMPLATE_ID=template_w46rrsu

# Your EmailJS Public Key (User ID)
# Found in: EmailJS Dashboard → Account → API Keys → Public Key
VITE_EMAILJS_PUBLIC_KEY=H_yDkjYt_zWVlbMZz

# Optional: Set to 'development' for additional logging
VITE_APP_ENV=development

# Instructions:
# 1. Copy this file to .env (without the .example extension)
# 2. Replace the placeholder values with your actual EmailJS credentials
# 3. Never commit the .env file to version control
# 4. For production deployment, set these variables in your hosting platform
